% Print-specific macros
% Dummy macros that make sense only for web version.
\newcommand{\lean}[1]{}
\newcommand{\discussion}[1]{}
\newcommand{\leanok}{}
\newcommand{\mathlibok}{}
\newcommand{\notready}{}
% Compatibility: content.tex uses \printbibliography (for biblatex in web build).
% For PDF we expand it to traditional bibtex commands.
\newcommand{\printbibliography}{\bibliographystyle{amsalpha}\bibliography{refs}}
% Make sure that arguments of \uses and \proves are real labels
\ExplSyntaxOn
\NewDocumentCommand{\uses}{m}
 {\clist_map_inline:nn{#1}{\vphantom{\ref{##1}}}%
  \ignorespaces}
\NewDocumentCommand{\proves}{m}
 {\clist_map_inline:nn{#1}{\vphantom{\ref{##1}}}%
  \ignorespaces}
\ExplSyntaxOff
