# Site settings

title: Graphons in Lean 4
email: freer@mit.edu
description: "Graphons, exchangeable structures, and Aldous–Hoover–Kallenberg theory in Lean 4"
# Conventional Jekyll split: `url` is the origin, `baseurl` the project path.
# Do NOT also pass `--baseurl` on the command line — doing both doubles the path and
# produces canonical URLs like `.../graphon/graphon/`.
baseurl: "/graphon"
url: "https://cameronfreer.github.io"
twitter_username:
github_username: cameronfreer
repository: cameronfreer/graphon

# Build settings
remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme
  - jekyll-github-metadata
